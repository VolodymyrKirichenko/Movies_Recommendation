import Button from '@mui/material/Button';

export const SubmitField = () => {
  return (
    <Button
      variant="contained"
      type="submit"
      size="large"
      sx={{ height: 'max-content' }}
    >
      Submit
    </Button>
  );
};
